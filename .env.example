# Copy to .env, or just run: npm run setup
DATABASE_URL=postgres://hms:hms@localhost:5432/hms
PORT=4000

# Optional. Where the 12-hourly backups go. Point this at a second drive or a
# mapped network folder: a backup on the same disk as the database is not a
# backup.
# BACKUP_DIR=D:\hms-backups
